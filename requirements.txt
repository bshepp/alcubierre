# Alcubierre / Path 2A scientific computing stack.
# Pinned so Google Colab and Hugging Face Jobs reproduce the author's environment.
# See README.md "Running the notebooks" for runtime guidance.

sympy==1.14.*
numpy==2.4.*
scipy==1.16.*
matplotlib==3.10.*

# Notebook execution + authoring.
nbformat>=5.10,<6
nbclient>=0.10,<0.11
jupyter-core>=5,<6
ipykernel>=6,<7

# Sweep I/O for HF Jobs parameter grids.
pandas>=2.3,<3
pyarrow>=17,<23

# Hub access from sweep modules / notebooks (kernel repos, Space logs,
# server-side bucket copies, etc.). Sweep dispatch via the `hf` CLI works
# without this, but in-process API calls require it. v1.x renumbering as of
# 2025-11; see https://huggingface.co/docs/huggingface_hub/release_notes.
huggingface_hub>=1.5,<2
