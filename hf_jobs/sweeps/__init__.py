"""Sweep modules. Each module exposes ``build_grid`` and ``evaluate``.

Imported dynamically by ``hf_jobs.run_sweep``. Actual sweep implementations
are added alongside their parent notebook (``israel_junction_partA`` for
Package 1, ``thickness_bound`` for Package 2, ``gw_recoil`` for Package 3).
"""
